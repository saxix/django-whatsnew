from django.conf.urls import patterns, include, url


urlpatterns = patterns(
    'whatsnew.views',
    # url(r'^(?P<version>)/$', 'release_note', name='whatsnew-release'),
    # url(r'^latest/$', 'latest', name='whatsnew-latest'),
)


